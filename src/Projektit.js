import React, { Component } from 'react';

const Projektit = () => {
    return(
        <div>
            <h2 style={{color:'blue'}}><u>Projects</u></h2>
            <p>.NET CORE School project in Github, program where you can set queries, answer them and see diagrams about answers</p>
        </div>
    );
}

export default Projektit; 